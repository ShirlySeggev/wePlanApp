
REPLACE SCSS FILE
ADD BoardMembers.JSX IN BOARD FOLDER IN CMPS





file:    BoardMenu.jsx
-------------------------------------------------------------------------------------------
new import:

import { BoardMembers } from './BoardMembers';
-------------------------------------------------------------------------------------------
   new function: 

 toggleMembers = () => {
        this.setState({ isMembers: !this.state.isMembers })
    }
-------------------------------------------------------------------------------------------
in render, const this.props:

toggleMembers, isMembers, board, onUpdateBoard

-------------------------------------------------------------------------------------------

in render:


<li onClick={toggleMembers} >Add a member</li>
                    {isMembers && <BoardMembers toggleMembers={toggleMembers} onUpdateBoard={onUpdateBoard} members={board.members} board={board} />}



---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

file:     BoardHeader.jsx
-------------------------------------------------------------------------------------------

new import:
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faPlus } from '@fortawesome/free-solid-svg-icons'

-------------------------------------------------------------------------------------------


new function:

   toggleMembers = () => {
        this.setState({ isMembers: !this.state.isMembers })
    }

-------------------------------------------------------------------------------------------

in render, BoardMenu tag:

                {toggleMenu && <BoardMenu board={board} toggleMembers={this.toggleMembers} isMembers={isMembers} toggleBoardMenu={this.toggleBoardMenu} onRemoveBoard={this.props.onRemoveBoard} onUpdateBoard={this.props.onUpdateBoard} />}

-------------------------------------------------------------------------------------------
in render:

                <div className="board-members">
                    <div className="board-header-add-member" onClick={() => {
                        this.toggleMembers()
                        this.toggleBoardMenu()
                    }}
                    >{<FontAwesomeIcon icon={faPlus} />}</div>
                    {members.map(member => <MemberAvatar member={member} key={member._id} />)}
                </div>

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

